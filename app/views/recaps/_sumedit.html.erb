<%= simple_form_for @recap, remote: true, html: { class: "d-none", data: { edit_recap_target: "form", action: "submit->edit-recap#update" } } do |f| %>
  <%= f.input :summary, as: :text %>
  <%= f.input :keypoint, label: "Key Points" %>
  <%= f.submit "Update", class: "btn-action" %>
<% end %>
